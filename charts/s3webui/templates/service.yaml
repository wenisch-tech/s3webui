apiVersion: v1
kind: Service
metadata:
  name: {{ include "s3webui.fullname" . }}
  labels:
    {{- include "s3webui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "s3webui.selectorLabels" . | nindent 4 }}
