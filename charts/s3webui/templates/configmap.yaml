apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "s3webui.fullname" . }}
  labels:
    {{- include "s3webui.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{- if $value }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
  {{- end }}
