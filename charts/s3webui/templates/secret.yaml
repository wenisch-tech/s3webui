{{- if .Values.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "s3webui.fullname" . }}
  labels:
    {{- include "s3webui.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
